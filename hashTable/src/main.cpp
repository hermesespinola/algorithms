#include <stdio.h>
#include <random>
#include <iostream>
#include <fstream>
#include "hashTable.h"

int main(int argc, char const *argv[]) {
  HashTable<int, int> *dir = new HashTable<int, int>();
  return 0;
}
